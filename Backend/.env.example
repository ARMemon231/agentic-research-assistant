# Copy this file to .env and fill in your real values
# NEVER commit .env to git — only commit .env.example

# MistralAPI
MISTRAL_API_KEY="Gusd*******************************"


# Qdrant Cloud (from cloud.qdrant.io)
QDRANT_URL=https://d6fd77******************************************************************
QDRANT_API_KEY=eyJhesd************************************************************************

# Collection name (must exist in Qdrant —)
QDRANT_COLLECTION=nlp_course_agentic
# TAVILY_API_KEY

TAVILY_API_KEY=tv**************************************************

# Embedding provider: "huggingface" (free, default) or "MistralAI" (paid)
EMBEDDING_PROVIDER=huggingface

# LLM model
LLM_MODEL=mistral-small-latest

# Number of chunks to retrieve per query
RETRIEVER_K=5
